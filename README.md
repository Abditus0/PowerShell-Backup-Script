# PowerShell-Backup-Script
This PowerShell script backs up all files from a specified source folder to a timestamped destination folder. It preserves the original subfolder structure, logs successes and errors, verifies the copy (by comparing file sizes), and includes duplicate filename handling as a safety net.
